* [x] `application.yml` 中 `server.port` 为 8081

* [x] `data_init.sql` 中的时段数据包含当前日期 (2026-04-16)

* [x] `OrderDetails.vue` 中的支付成功后能自动加载时段列表

* [x] `OrderDetails.vue` 选择时段后能正确加载该时段下的可用座位

* [x] `OrderDetails.vue` 预约成功后显示预约码

* [x] `full_link_test.py` 自动化测试全流程通过

* [x] 扫码核销接口 (`/api/v1/admin/pickup-qrcodes/scan`) 能够正确核销预约记录并更新订单状态

